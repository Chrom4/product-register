import { View, Text, StyleSheet } from "react-native";

const Layout = () => {
  return (
    <View>
      <Text>Teste</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default Layout;
